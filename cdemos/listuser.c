#include <stdio.h>
#include <stdlib.h>
#include "metalist.h"
#include "linkedlist.h"

int main() {
    LList *mylist = init();
    mylist = addToFront(mylist,5);
    mylist = addToFront(mylist,7);
    
}

